/**
 * This function retuirn Hello ..
 * @param name
 * @returns Hello and Name which passed
 */
export function greet(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Test Method is being called
 * @param name
 * @returns
 */
export function test(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Test2 method is being called
 * @param name
 * @returns
 */
export function test2(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Test3 method is being called
 * @param name
 * @returns
 */

export function test3(name: string): string {
  return `Hello, ${name}!`;
}

/**
 * Test4 method is being called
 * @param name
 * @returns
 */

export function test4(name: string): string {
  return `Hello, ${name}!`;
}
